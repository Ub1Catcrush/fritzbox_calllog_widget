<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp">

    <!-- Live color preview swatch -->
    <View
        android:id="@+id/color_preview"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginBottom="16dp" />

    <!-- Hex input -->
    <EditText
        android:id="@+id/et_hex"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="#AARRGGBB"
        android:inputType="text"
        android:maxLength="9"
        android:fontFamily="monospace"
        android:textSize="14sp"
        android:layout_marginBottom="16dp" />

    <!-- Alpha -->
    <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content" android:orientation="horizontal" android:layout_marginBottom="4dp">
        <TextView android:id="@+id/label_a" android:layout_width="48dp" android:layout_height="wrap_content" android:text="A: 255" android:textSize="12sp"/>
        <SeekBar android:id="@+id/seek_a" android:layout_width="0dp" android:layout_height="wrap_content" android:layout_weight="1" android:max="255"/>
    </LinearLayout>

    <!-- Red -->
    <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content" android:orientation="horizontal" android:layout_marginBottom="4dp">
        <TextView android:id="@+id/label_r" android:layout_width="48dp" android:layout_height="wrap_content" android:text="R: 0" android:textSize="12sp"/>
        <SeekBar android:id="@+id/seek_r" android:layout_width="0dp" android:layout_height="wrap_content" android:layout_weight="1" android:max="255"/>
    </LinearLayout>

    <!-- Green -->
    <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content" android:orientation="horizontal" android:layout_marginBottom="4dp">
        <TextView android:id="@+id/label_g" android:layout_width="48dp" android:layout_height="wrap_content" android:text="G: 0" android:textSize="12sp"/>
        <SeekBar android:id="@+id/seek_g" android:layout_width="0dp" android:layout_height="wrap_content" android:layout_weight="1" android:max="255"/>
    </LinearLayout>

    <!-- Blue -->
    <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content" android:orientation="horizontal">
        <TextView android:id="@+id/label_b" android:layout_width="48dp" android:layout_height="wrap_content" android:text="B: 0" android:textSize="12sp"/>
        <SeekBar android:id="@+id/seek_b" android:layout_width="0dp" android:layout_height="wrap_content" android:layout_weight="1" android:max="255"/>
    </LinearLayout>

</LinearLayout>
