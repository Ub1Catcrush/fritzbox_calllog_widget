Three independent connection profiles: LAN TR-064, Internet TR-064, and Internet MyFRITZ, each with its own host, port, and HTTPS setting. Drag-to-reorder priority list with per-profile enable/disable. MyFRITZ login now supports Protocol v2 (PBKDF2-SHA256) with automatic v1 fallback. Step-by-step connectivity checker with live status per profile. Widget shows cached call list while a background refresh runs. Failed refreshes appear as a subtle overlay rather than replacing the list.
